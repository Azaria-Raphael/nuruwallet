// Smoke test: verifies the app shell renders without throwing.
// Requires ProviderScope and an in-memory database (full widget tests in Phase 2).
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder - widget tests added in Phase 2', () {
    expect(true, isTrue);
  });
}
